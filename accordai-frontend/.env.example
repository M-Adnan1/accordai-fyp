# Backend origin for production builds (Vercel etc.). Leave UNSET in local dev —
# the code then falls back to '/api', which the Vite proxy forwards to
# localhost:8000. No trailing slash, no /api suffix.
# Example: VITE_API_BASE_URL=https://your-tunnel.ngrok-free.dev
# VITE_API_BASE_URL=