# No env vars currently required. Auth is JWT-based: the session token is
# stored in localStorage and sent as an Authorization header; tenant context
# is derived server-side from the authenticated user.